## Learned user preferences

- Unslop is always on: follow `~/.agents/skills/unslop/SKILL.md`. Machine-global rule at `~/.cursor/rules/unslop.mdc` (`alwaysApply`) so it applies in every workspace; project rule in this repo is backup.
- Wants practical, command-level troubleshooting from logs and config before recommendations, not high-level theory.
- Avoids heavy always-on Python monitor stacks for the TURZX smart screen; wants low CPU for always-on hardware (smart screen, sensors, and similar). Fine with a lightweight custom PIL dashboard (~1s refresh) or something non-Python.
- Prefers letterbox scaling and integer pixel nudges for TURZX layout. Fractional offsets hit dead zones and jump.
- Prefers Caelestia's live dynamic scheme to drive TURZX colours, Cursor theme, Zen Browser UI, and the SDDM greeter wallpaper + palette. No separate fixed themes.
- Prefers the Caelestia session / logout drawer on the left main monitor (DP-3), not the right display. Wants Super+W monitor-aware browsers: Brave Origin on DP-3 via `browserByMonitor`, Helium elsewhere (`browser` is `helium` in `~/.config/caelestia/hypr-vars.lua`; keybind in `~/.config/hypr/hyprland/keybinds.lua`). Prefers Helium's vertical-tab hover (stays open past the window edge) over Brave Origin. Helium vertical tabs follow the focused window's monitor via `~/.local/bin/helium-hypr-layout` (left on DP-3, right on HDMI-A-1; diary `Helium-hypr-layout/`); launch Helium through `~/.local/bin/helium` so CDP port 9333 is on. Helium needs a polite quit or it restores tabs as crashed: Super+Q sends Alt+F4 for Helium only; Shutdown / Reboot / Logout / Hibernate run `~/.local/bin/graceful-quit-helium` first via `~/.config/caelestia/shell.json`.
- Fish interactive shell: home-wide fzf file launcher on Ctrl-F; cwd fzf on Ctrl-T after loading `fzf_key_bindings` in `~/.config/fish/config.fish`. `cat` aliased to `bat --paging=never` when bat is installed; Caelestia startup still uses `/bin/cat` directly.
- Prefers diary topics as `README.md` (GitHub-visible) written so a stranger can catch up cold; related config files copied into the same topic folder; avoid hardcoding machine-specific Zen profile directory names in checks/docs. Full write-up process: `.cursor/diary-writing.md` (local, gitignored).
- TURZX idle is the stats dashboard at ~1 Hz. Super+Shift+F overlays speedtest (TURZX panel only, no desktop overlay; press again to abort). Ambient cycle, Super+Shift+D peek, and Super+Shift+N are off the boot path (USB resets when JPEG flooded the hub shared with audio). TURZX binds live in `~/.config/caelestia/hypr-user.lua`. Interested in higher refresh where feasible (dual-rate JPEG/PNG; H.264 clip path proven for a later live-encoder experiment).
- Wants VRR off: tested adaptive sync and removed it after game flicker; keep `misc.vrr=0` and no per-monitor `vrr`.
- Prefers MangoHud slim overlay for non-ACE titles: live `fps` plus `fps_metrics=avg` and frametime (ms only draws next to live `fps`, not AVG alone), `frame_timing=0`, no CPU/GPU/VRAM (`~/.config/MangoHud/MangoHud.conf`). WuWa on Steam: no MangoHud, in-process overlays, or launch wrappers (ACE); keep Steam launch options empty; use external `~/.local/bin/wuwa-session-log`, `~/.local/bin/wuwa-boost` (renice/ionice only), and Steam F2 for FPS. Mid-WuWa: never start/kill hyprsunset (NVIDIA+HDR CTM hang); Alt+F1 (`~/.config/hypr/scripts/toggle_night_light.sh`) cycles ASUS DP-3 DDC backlight only; set Kelvin before launch.
- Prefers game installs on native btrfs (e.g. WuWa on SATA SSD under `~/.local/share/Steam`) over Kingston NVMe NTFS FUSE `/mnt/nvme`; moving WuWa there is worse until that NVMe has native btrfs/ext4.

## Learned workspace facts

- Linux (CachyOS), Hyprland, Fish (`~/.config/fish`) and Zsh. CPU: Ryzen 9 7900X. Peripherals: Rainy 75 keyboard (`320f:5055`), MCHOSE L7 Pro mouse, MSI B650 Mystic Light. OpenRGB handles board RGB; Rainy 75 needs FN keys or ZMK firmware. Proton Drive via user unit `rclone-proton.service` at `~/ProtonDrive`. This repo is the Hyprland troubleshooting diary (`Hyprland_Diary`); git `origin` is Cursor (`https://origin.cursor.com/theadenkingof/Hyprland_Diary.git`).
- TURZX 8" USB smart screen (`1cbe:0080`, TURZX1.0), TUR_USB protocol. Mounted landscape-wide. Firmware framebuffer is portrait 800×1280; library portrait/landscape labels do not match the glass. Windows/cold-plug leave the panel expecting an 800×1280 stream (raw 1280×800 shows a sideways strip + leftover TURZX V2).
- Custom dashboard at `~/Documents/dashboard` (1280×800 PIL; `CONTENT_ROTATE = 0`; `Orientation.LANDSCAPE`). Library rotates 270° before USB so the wire size is 800×1280. Picture card uses `~/Pictures/4.png` with near-black keyed transparent. Storage card tracks SSD (`/`), NVMe (`/mnt/nvme`), and HDD (`/mnt/hdd`). Bonsai panel shows monthly WiFi usage (wlan0) with historical average; state at `~/.local/state/turzx/wifi_usage.json`. Weather card: OpenWeatherMap, city 3019952 (Éragny); key in `~/.config/turzx/config.json` and calendar `.env` path `weather.py` searches. Started by `turzx-dashboard.service` on boot (`PYTHONUNBUFFERED=1`). Views: stats + speedtest overlay; ambient / peek / game-mode code unused on disk. GPU card shows util alone on the main line; detail line is temp left of VRAM numbers without a `VRAM` label. USB hub `1-5` shared with audio re-enumerates under GPU load; `usb_guard.py` waits ~1.5s settle before reopen, catches all USB writes (including SetBrightness) to stop systemd crash spirals, and at GPU ≥40% slows USB uploads to 5s. Diary: `TURZX-SCREEN/ambient-screens/`, `TURZX-SCREEN/usb-stability/`.
- `turing-smart-screen-python` at `~/Documents/turing-smart-screen-python` (TUR_USB). Keep stock `DisplayPILImage` USB rotate for LANDSCAPE (do not skip it). Full-frame JPEG/PNG over USB tops out around ~5 fps; dirty-rect still uploads the full canvas on the public path. H.264 chunk stream paints smoothly; live experiment `turzx_h264_live.py` (NVENC → Annex-B → cmds 17/121/122/123) is opt-in, not the default service. Spike notes in `TURZX-SCREEN/refresh-upgrade-spike/`.
- GPU: NVIDIA RTX 4070 SUPER (12 GB / 12282 MiB VRAM), stats via `nvidia-smi`. PenguinBurner 0.7.9 in `~/.venvs/penguin-burner` (`penguin-burner`/`pburn` GUI, `penguin-burner-cli`); `penguin-burnerd` disabled at boot (manual `sudo systemctl start` when needed); in-game overlay is Steam-only.
- Caelestia dots for Hyprland theming and SDDM greeter (`pixie-caelestia` on Wayland/kwin; wallpaper/colours via `caelestia-sddm-sync.service`; DP-3 primary via `kwinoutputconfig.json`; helper `~/.local/bin/caelestia-sddm-monitors`). TURZX watches `~/.local/state/caelestia/scheme.json` (Everforest fallback). Cursor via `~/.local/bin/caelestia-cursor`. Zen via `caelestia-zen-sync` + profile `chrome/` CSS; Zen needs a full restart and both can lag after wallpaper changes.
- Wuthering Waves on Steam (3513350): proton-cachyos-slr (switched from DW-Proton Latest; Steam can drift back to DW-Proton), SATA SSD btrfs at `~/.local/share/Steam/steamapps/common/Wuthering Waves`, prefix `compatdata/3513350/pfx`. ACE blocks MangoHud/in-process overlays and launch wrappers; keep Steam launch options empty (`DXVK_ASYNC` is a no-op on DX12/vkd3d; `PROTON_NO_FSYNC=0` is a no-op). External `~/.local/bin/wuwa-session-log` (CSV/summary under `~/.local/state/wuwa/sessions/`) and `~/.local/bin/wuwa-boost` (renice/ionice, no LD_PRELOAD/gamemoderun); Steam F2 for FPS. Leave DLSS Frame Generation off on Proton (silent fail; cutscenes drop below 60 while GPU idles). FPS stickiness: launch rewrites `GameUserSettings.ini` `FrameRateLimit` from `LocalStorage.db` `CustomFrameRate` (preset indices like 60/120); arbitrary caps such as 90 in the ini do not stick. RT low/full under real play pins GPU (~88%) like full RT; no-RT Lahai-Roi ~54% GPU avg, still CPU/streaming bound (~347% WuWa CPU, `FAsyncLoadingTh`). Lahai-Roi is the worst-optimized zone. Nsight via pacman (`nsys-ui`); capture script `~/.local/bin/wuwa-nsys-lahai-roi`; old Lutris copy on Kingston NVMe NTFS FUSE `/mnt/nvme` (worse than SATA btrfs; native btrfs/ext4 on that NVMe would be the real upgrade).
- Dual monitors: DP-3 1440p left (main, scale 1.0 in `~/.config/hypr/hyprland-gui.lua` for native-res XWayland gaming), HDMI-A-1 1080p right. HyprMod `hyprland-gui.lua` loads after `hypr-user.lua` and wins on conflicting monitor settings. hyprfocus in `hypr-user.lua` flashes keyboard focus on monitor jump only (`only_on_monitor_change = true`; mouse off). Ctrl+Alt+Delete focuses DP-3 then opens the session menu. Logout uses `hyprshutdown --vt 1` so the greeter returns. logind `Terminate` SIGABRTs `start-hyprland` on this stack. Night light: Alt+F1 → `~/.config/hypr/scripts/toggle_night_light.sh`; `hyprsunset -i` at login; Kelvin 5500→5000→4500→4000→off (`identity`, no pkill); mid-WuWa Alt+F1 is DP-3 DDC backlight only (80/65/50/35). hyprpm `dynamic-cursors`: autoload via `hyprpm reload -n` at top of `~/.config/hypr/hyprland/execs.lua` `hyprland.start`; permission in `~/.config/hypr/hyprland.lua`; tune in `~/.config/caelestia/hypr-user.lua` under `plugin.dynamic_cursors`.
- Sunshine (Moonlight host): user unit `~/.config/systemd/user/sunshine.service` must be `WantedBy=graphical-session.target` (not `default.target`) so `WAYLAND_DISPLAY` exists; otherwise it falls back to KMS and fails `output_name = DP-3`. UI https://localhost:47990; NVENC on DP-3.
- Speedtest (Super+Shift+F): Fast.com engine via `~/.local/bin/network-speedtest` + `speedtest-session`; state at `~/.local/state/turzx/speedtest.json`; dashboard swaps to dual gauges for the run and holds ~3s. Toggle script `~/.config/hypr/scripts/toggle_speedtest.sh`. Diary topic `Speedtest-widget/`. AUR `fast-git` is a separate CLI, not the widget engine.
- Primary Super launcher is Caelestia (`caelestia:launcher`). Overlay at `~/.config/quickshell/caelestia` → `~/orca/projects/Hyprland_Laucnher/shell` adds Flow-style rows: Elephant files (`fd` fallback), Qalculator calc/convert, URL/websearch, PATH runner, Hyprland window focus via Lua `hl.dsp.focus`. Runner launches need Ghostty `-e` before `wrap_term_launch.sh` or Ghostty treats args as config keys. Window focus uses Lua dispatch, not classic `focuswindow`. Orca IDE alongside Cursor. Diary: `Caelestia-launcher-flow/`. Fuzzel stays for clipboard/emoji; Walker for cheatsheet (`CTRL + SPACE`).

